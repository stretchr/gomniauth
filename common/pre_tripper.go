package common

/*
// PreTripper represents an object that gets to modify a
// request before a round trip is made.
type PreTripper interface {
	// PreRoundTrip is called before the round trip is made.
	PreRoundTrip(tripper Tripper) error
}
*/
